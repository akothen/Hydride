#ifndef sobel3x3_batch_0058_sample_0015_SCHEDULE_H
#define sobel3x3_batch_0058_sample_0015_SCHEDULE_H

// MACHINE GENERATED -- DO NOT EDIT
// This schedule was automatically generated by Adams2019
// for target=x86-64-linux-avx-avx2-disable_llvm_loop_opt-f16c-fma-sse41  // NOLINT
// with machine_params=32,24000000,40

#include "Halide.h"


inline void apply_schedule_sobel3x3_batch_0058_sample_0015(
    ::Halide::Pipeline pipeline,
    ::Halide::Target target
) {
    using ::Halide::Func;
    using ::Halide::MemoryType;
    using ::Halide::RVar;
    using ::Halide::TailStrategy;
    using ::Halide::Var;
    Func output = pipeline.get_func(9);
    Func sobel_y = pipeline.get_func(8);
    Func sobel_y_avg = pipeline.get_func(7);
    Func sobel_x = pipeline.get_func(6);
    Func sobel_x_avg = pipeline.get_func(5);
    Func input_16 = pipeline.get_func(4);
    Func bounded_input = pipeline.get_func(3);
    Func repeat_edge = pipeline.get_func(2);
    Func lambda_0 = pipeline.get_func(1);
    Var _0(repeat_edge.get_schedule().dims()[0].var);
    Var _0i("_0i");
    Var _1(repeat_edge.get_schedule().dims()[1].var);
    Var x(output.get_schedule().dims()[0].var);
    Var xi("xi");
    Var xii("xii");
    Var y(output.get_schedule().dims()[1].var);
    Var yi("yi");
    Var yii("yii");
    output
        .split(y, y, yi, 36, TailStrategy::ShiftInwards)
        .split(yi, yi, yii, 9, TailStrategy::ShiftInwards)
        .split(x, x, xi, 64, TailStrategy::ShiftInwards)
        .split(xi, xi, xii, 32, TailStrategy::ShiftInwards)
        .unroll(xi)
        .vectorize(xii)
        .compute_root()
        .reorder({xii, xi, x, yii, yi, y})
        .parallel(y);
    sobel_y
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 128, TailStrategy::RoundUp)
        .split(xi, xi, xii, 16, TailStrategy::RoundUp)
        .unroll(xi)
        .vectorize(xii)
        .compute_at(output, yii)
        .reorder({xii, xi, x, y});
    sobel_y_avg
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 16, TailStrategy::RoundUp)
        .unroll(x)
        .vectorize(xi)
        .compute_at(sobel_y, x)
        .reorder({xi, x, y});
    sobel_x
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 16, TailStrategy::RoundUp)
        .unroll(x)
        .vectorize(xi)
        .compute_at(output, x)
        .reorder({xi, x, y});
    sobel_x_avg
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 16, TailStrategy::RoundUp)
        .unroll(x)
        .unroll(y)
        .vectorize(xi)
        .compute_at(output, x)
        .reorder({xi, x, y});
    input_16
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 32, TailStrategy::RoundUp)
        .unroll(y)
        .vectorize(xi)
        .compute_at(output, yi)
        .store_at(output, y)
        .reorder({xi, y, x});
    bounded_input
        .store_in(MemoryType::Stack)
        .split(x, x, xi, 32, TailStrategy::RoundUp)
        .unroll(y)
        .vectorize(xi)
        .compute_at(input_16, x)
        .reorder({xi, x, y});
    repeat_edge
        .store_in(MemoryType::Stack)
        .split(_0, _0, _0i, 32, TailStrategy::ShiftInwards)
        .vectorize(_0i)
        .compute_at(output, yi)
        .store_at(output, y)
        .reorder({_0i, _0, _1});

}

#endif  // sobel3x3_batch_0058_sample_0015_SCHEDULE_H

#ifndef max_pool_batch_0086_sample_0002_SCHEDULE_H
#define max_pool_batch_0086_sample_0002_SCHEDULE_H

// MACHINE GENERATED -- DO NOT EDIT
// This schedule was automatically generated by Adams2019
// for target=x86-64-linux-avx-avx2-disable_llvm_loop_opt-f16c-fma-sse41  // NOLINT
// with machine_params=32,24000000,40

#include "Halide.h"


inline void apply_schedule_max_pool_batch_0086_sample_0002(
    ::Halide::Pipeline pipeline,
    ::Halide::Target target
) {
    using ::Halide::Func;
    using ::Halide::MemoryType;
    using ::Halide::RVar;
    using ::Halide::TailStrategy;
    using ::Halide::Var;
    Func output = pipeline.get_func(3);
    Func maximum = pipeline.get_func(2);
    Func input_bounded = pipeline.get_func(1);
    Var b(output.get_schedule().dims()[3].var);
    Var c(output.get_schedule().dims()[0].var);
    Var ci("ci");
    Var x(output.get_schedule().dims()[1].var);
    Var xi("xi");
    Var y(output.get_schedule().dims()[2].var);
    Var yi("yi");
    RVar r12_x(maximum.update(0).get_schedule().dims()[0].var);
    RVar r12_y(maximum.update(0).get_schedule().dims()[1].var);
    output
        .split(x, x, xi, 2, TailStrategy::ShiftInwards)
        .split(y, y, yi, 11, TailStrategy::ShiftInwards)
        .split(c, c, ci, 32, TailStrategy::ShiftInwards)
        .vectorize(ci)
        .compute_root()
        .reorder({ci, c, xi, yi, x, y, b})
        .fuse(x, y, x)
        .parallel(x);
    maximum.update(0)
        .split(c, c, ci, 32, TailStrategy::RoundUp)
        .vectorize(ci)
        .reorder({ci, r12_x, r12_y, c, x, y, b});
    maximum
        .store_in(MemoryType::Stack)
        .split(c, c, ci, 32, TailStrategy::RoundUp)
        .vectorize(ci)
        .compute_at(output, yi)
        .reorder({ci, c, x, y, b});
    input_bounded
        .split(c, c, ci, 32, TailStrategy::ShiftInwards)
        .vectorize(ci)
        .compute_at(output, yi)
        .store_at(output, x)
        .reorder({ci, c, x, y, b});

}

#endif  // max_pool_batch_0086_sample_0002_SCHEDULE_H
